apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: groq-chatbot
  labels:
    app: groq-chatbot
    app.kubernetes.io/part-of: genai-app
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: groq-chatbot