apiVersion: v1
kind: Service
metadata:
  name: groq-chatbot
  labels:
    app: groq-chatbot
    app.kubernetes.io/part-of: genai-app
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: groq-chatbot
  type: ClusterIP